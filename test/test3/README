Goal: Test the capabilities of sock.read()

Steps/Requirements:
1. Client sends server multiple sets of character data
2. Server prints out data
3. Server sends client data
4. Client reads and prints data

STATUS: SEMI-COMPLETE. SEE NOTES:

NOTES:
The amount of data read by the function `read` must be the same
size as the as the data sent. A.K.A it will block until all 
data that was requested to be read is read...
